#ifndef TYPE_H_
#define TYPE_H_

#include <string.h>

// #define M_PI 3.14159265358979323846

int isoperator(char* token);
int isfunction(char* token);
int isalpha(int token);
int isdigit(int token);

#endif